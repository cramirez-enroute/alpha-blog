<div class="title">
  <h1>Articles</h1>
</div>
<div class="table-wrapper">
  <table class="fl-table">
    <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3">Actions</th>
    </tr>
    </thead>
    <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.description %></td>
        <td>
          <%= button_to "", { action: "show", id: article.id, class: 'gc-button gc-icon-button' }, { method: :get } do %>
            <i class="uil uil-eye"></i>
          <% end %>
        </td>
        <td>
          <%= button_to "", { action: "destroy", id: article.id, class: 'gc-button gc-icon-button' }, method: :delete do %>
            <i class="uil uil-trash-alt"></i>
          <% end %>
        </td>
        <td>
          <%= button_to "", { action: "edit", id: article.id, class: 'gc-button gc-icon-button' }, { method: :get } do %>
            <i class="uil uil-pen"></i>
          <% end %>
        </td>
      </tr>
    <% end %>
    <tbody>
  </table>
</div>

<%= button_to "New article", { action: "new"}, { method: :get, class: 'gc-button' } %>